Variables not being set?
I'm trying to create a little algorithm that(needs improving) will determine in a set of numbers, what the percentage of the numbers were in ascending order and descending order. I have a little maths at the end to figure out the percentages.   double descendingAverage = (tempDescendingList.Count / (tempAscendingList.Count + tempDescendingList.Count )) * 100; double asendingAverage = (tempAscendingList.Count / (tempAscendingList.Count + tempDescendingList.Count )) * 100;  but each double gets assigned to 0, even when i debug it shows that each .count is equal to 3, 'm using a list of 7 so the maths is off because there should be .count equaling 3 and 4 but thats a simple fix. here is all the code set up ready to copy and paste into a console app   static int[] numbers = { 1, 2, 3, 4, 3, 2, 1}; static void Main(string[] args) { ExtractFlow(); } private static void ExtractFlow() { List tempDescendingList = new List(); List tempAscendingList = new List(); for (int i = 0; i < numbers.Length; i++) { if (i + 1 < numbers.Length) { if (numbers[i] < numbers[i + 1]) { tempAscendingList.Add(numbers[i]); } else { tempDescendingList.Add(i); } }else if (i == numbers.Length) { if (numbers[i] < numbers[i - 1]) { tempDescendingList.Add(numbers[i]); } else { tempDescendingList.Add(numbers[i]); } } } double descendingAverage = (tempDescendingList.Count / (tempAscendingList.Count + tempDescendingList.Count )) * 100; double asendingAverage = (tempAscendingList.Count / (tempAscendingList.Count + tempDescendingList.Count )) * 100; Console.WriteLine("Asending average : " + asendingAverage); Console.WriteLine("Desending average : " + descendingAverage); Console.ReadLine(); }       submitted by /u/z-2 [link] [comments] 