Error creating VM in Avail Set
I keep getting an error trying to create a VM in an Availability Set. I created a Managed Availability Set. I am using Premium LRS storage, and I keep getting this error: ErrorMessage: Addition of a VM with managed disks to non-managed Availability Set or addition of a VM with blob based disks to managed Availability Set is not supported. Please create an Availability Set with 'Aligned' SKU in order to add a VM with managed disks to it. StatusCode: 409 ReasonPhrase: Conflict Here is part of the creating script: $storageType = "Premium_LRS" $vmSize = "Standard_DS3_v2" $osDiskUri = "https://storageurl/vhds/SOMEVM-OS.vhd" $AvailabilitySet = Get-AzureRmAvailabilitySet -ResourceGroupName $rgName -Name "MY-AVAILSET" $vm = New-AzureRmVMConfig -VMName $vmName -VMSize $vmSize -AvailabilitySetID $AvailabilitySet.Id $vm = Set-AzureRmVMOSDisk -VM $vm -Name $osDiskName -VhdUri $osDiskUri -CreateOption FromImage $vm = Set-AzureRmVMOperatingSystem -VM $vm -Linux -ComputerName $computerName -Credential $cred $vm = Set-AzureRmVMSourceImage -VM $vm -PublisherName "RedHat" -Offer "RHEL" -Skus "7.3" -Version "latest" $vm = Add-AzureRmVMNetworkInterface -VM $vm -Id $nic.Id New-AzureRmVM -ResourceGroupName $rgName -Location $location -VM $vm -Verbose Solved! Thank you, /u/Gabrielmccoll  submitted by /u/thefueley [link] [comments] 